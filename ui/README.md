# UI

This is a basic UI , the actual frontend is written in react [here](./frontend) and the backend in python

## Install
```bash
uv sync
```

## Usage
```bash
# Wait for browser tab to open after running
python main.py
```